<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="apps.my.p2017">

    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_PRIVILEGED" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name" />
        <activity android:name=".BottomButton" />
        <activity
            android:name=".TrackFood"
            android:label="@string/title_activity_track_food" />
        <activity
            android:name=".BottomActivity"
            android:label="@string/title_activity_bottom" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".CreateFoodActivity" />
        <activity android:name=".LogFoodActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".FoodLogsActivity" />
        <activity android:name=".FoodSearch" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".RegisterActivity"
            android:label="@string/title_activity_register"
            android:theme="@style/AppTheme" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".GoalsActivity" />
        <activity android:name=".ExpandableTest" />
        <activity
            android:name=".EnergyFit"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_splash"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LogExerciseActivity" />
        <activity
            android:name=".LogYourExerciseActivity"
            android:label="@string/title_activity_log_your_exercise"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".LogPhysicalActivity"
            android:label="@string/title_activity_log_physical"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".PickYourExercise"
            android:label="@string/title_activity_pick_your_exercise"
            android:theme="@style/AppTheme" />
        <activity android:name=".SearchExerciseActivity" />
        <activity android:name=".DeviceScanActivity" />
        <activity android:name=".YourDevicesActivity" />
        <activity android:name=".ConfigActivity" />
        <activity
            android:name=".TestMVVM"
            android:label="@string/title_activity_test_mvvm"
            android:theme="@style/AppTheme" />
        <activity android:name=".CreateFoodMenuActivity" />
        <activity android:name=".DataBindingTutorial" />
        <activity android:name=".PieCharting" />
        <activity android:name=".Bullshit" />
        <activity android:name=".MacroGoalsActivity" />
        <activity android:name=".TrackExercise" />
        <activity android:name=".WeighStatusActivity" />
        <activity android:name=".EditOrRemoveExercise" />
        <activity android:name=".RealTimeHeartRateActivity" />
        <activity android:name=".Main2Activity"></activity>
    </application>

</manifest>